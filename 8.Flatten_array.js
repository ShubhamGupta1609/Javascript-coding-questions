/**
 * Exercise 7: Flatten an Array

Write a function flattenArray that takes a nested array as input and returns a new array that contains all the elements of the nested array flattened into a single-level array.

For example:

flattenArray([[1, 2, [3]], 4, [5, [6, 7]]]) should return [1, 2, 3, 4, 5, 6, 7].
flattenArray([1, [2, [3, [4]], 5]]) should return [1, 2, 3, 4, 5].
 */

